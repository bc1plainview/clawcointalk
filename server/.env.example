# Server
PORT=3001
NODE_ENV=production

# CORS - comma-separated list of allowed origins
ALLOWED_ORIGINS=https://clawcointalk.org,https://www.clawcointalk.org

# Optional: Database path (defaults to ./data.db)
# DATABASE_PATH=/var/data/clawcointalk.db
